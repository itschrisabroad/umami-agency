import { PoseFactoryConfig, PoserConfig, Poser, PoseMap, TransitionMap } from './types';
import { eachValue, fromPose } from './inc/transition-composers';
declare const poseFactory: <V, A, C, P>({ getDefaultProps, defaultTransitions, bindOnChange, startAction, stopAction, readValue, readValueFromSource, resolveTarget, createValue, convertValue, getInstantTransition, getTransitionProps, addActionDelay, selectValueToRead, convertTransitionDefinition, transformPose, posePriority, extendAPI }: PoseFactoryConfig<V, A, C, P>) => (config: PoserConfig<V>) => Poser<V, A, C, P>;
export default poseFactory;
export { Poser, PoserConfig, PoseMap, TransitionMap, eachValue, fromPose };
